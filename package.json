{
  "name": "aeg-poc",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "scripts": "yarn workspace @aeg-poc/scripts run serve",
    "build": "yarn scripts build",
    "start:api": "yarn scripts start"
  }
}